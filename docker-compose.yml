version: "3.8"
services:
  hugo:
    image: klakegg/hugo:ext-alpine
    command: server --bind=0.0.0.0 --disableFastRender --poll 500ms
    ports:
      - "1313:1313"
    volumes:
      - ./:/src
    environment:
      - HUGO_ENV=development
